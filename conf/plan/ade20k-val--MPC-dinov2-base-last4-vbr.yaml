# @package _global_
defaults:
  - /args: default
  - /dataloader: default
  - /data: ade20k_val
  - /model: MPC-dinov2-base-last4-vbr
  - /heads: dinov2_head
  - /metrics:
    - ade20k_seg_metrics
  - _self_

name: ade20k-val--MPC-dinov2-base-last4-vbr
description: "data: ADE20K val; model: MPC2-v3 VBR + DINOv2-base last4 layers + ade20k_seg_base_last4; task: segmentation"
dataset: ${datasets.ade20k_val}
test_transforms:
  - type: cofai.transforms.PadImage
    size: 512
    keys: [img, semseg]
  - type: cofai.transforms.PadToMultiple
    multiple: 64
    keys: [img, semseg]
  - type: cofai.transforms.AddIgnoreRegions
  - type: cofai.transforms.ToTensor
    keys: [img, semseg]
collate:
  mode: stack_only
task_configs:
  - label: semseg
    meter: ${metrics.ade20k_seg_metrics}
load:
  path: ${PROJECT_ROOT}/weights/MPC/MPC2-v3-base-vbr-pruned.pth.tar
  strict: false
  rules:
    - ["startswith", "dino.model", "useless"]
    - ["startswith", "heads.", "useless"]
model:
  type: MPC_I2
  dino_backbone:
    type: cofai.backbone.Dinov2TimmBackbone
    model_size: base
    img_size: 512
    patch_size: 16
    dynamic_size: true
    slot: -3
    n_last_blocks: 4
  dino_codec:
    type: cofai.latent_codecs.vit_feature_codec.VbrVitUnionLatentCodec
    h_dim: 768
    y_dim: 48
    z_dim: 48
    groups: 16
  heads:
    semseg: ${heads.ade20k_seg_base_last4}

multi_run:
  0:
  16:
  32:
  48:
  64:
